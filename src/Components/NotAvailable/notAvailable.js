import React from 'react';
import './NotAvailable.css'

export default function NotAvailable() {
    return (
        <div className='not-available-container'>
            <h1 className='not-available'>No movie available for selected genre</h1>
        </div>
    );
};

